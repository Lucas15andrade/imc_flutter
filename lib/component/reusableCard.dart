import 'package:flutter/material.dart';

class ReusableCard extends StatelessWidget {

  String _title;
  bool _pressed;


  ReusableCard(this._title, this._pressed);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
